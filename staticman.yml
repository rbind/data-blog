comments:
  allowedFields: ["path", "name", "email", "message"] # path doesn't work?
  branch: "master"
  commitMessage: "You have a new comment on your website."
  extension: "md"
  filename: "{@id}"
  format: "frontmatter"
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
  moderation: true
  name: "www.datascienceblog.net"
  notifications:
    enabled: false
  path: "data/comments"
  requiredFields: ["path", "name", "email", "message"] # path doesn't work?
  transforms:
    body: "frontmatterContent"
    email: md5
